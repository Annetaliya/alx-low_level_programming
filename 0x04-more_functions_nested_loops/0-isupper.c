#include "main.h"
/**
 *_isupper - check if a character is uypper or not
 *@c: character to be tested
 *Return: 1 whether it is 0 or otherwise
 *
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
	{
		return (0);
	}
	return (0);
}
